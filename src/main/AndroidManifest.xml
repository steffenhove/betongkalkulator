<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BetongKalkulator"
        tools:targetApi="31">

        <!-- Start Activity -->
        <activity
            android:name="no.steffenhove.betongkalkulator.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name="no.steffenhove.betongkalkulator.MainActivity"
            android:exported="false" />

        <!-- History Activity -->
        <activity
            android:name="no.steffenhove.betongkalkulator.HistoryActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name="no.steffenhove.betongkalkulator.SettingsActivity"
            android:exported="false" />

    </application>

</manifest>
